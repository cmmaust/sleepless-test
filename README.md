# Sleepless Test Website
A static html one page website for Sleepless Media.

# To use this site locally:
- Pull down the repo
- In the root, open the command line and run:
  - `npm i && gulp`
  - `gulp watch` is also available as a command
